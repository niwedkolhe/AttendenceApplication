<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itsniv.attendenceapplication">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/felix"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".view.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Login"
            android:label="@string/login">
        </activity>



        <activity android:name=".view.Register" android:label="@string/register"></activity>
        <activity android:name=".view.Menu" android:label="MENU"></activity>
        <activity android:name=".view.ResetPasswordActivity"></activity>

    </application>

</manifest>